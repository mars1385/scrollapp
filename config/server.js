// using access keys from api unsplash
const applicationId = 'YOUR_API_KEY';
const secret = 'YOUR_API_SECRET_KEY';
const callbackUrl = 'http://localhost/3000';

// export access keys
module.exports = {
    applicationId,
    secret,
    callbackUrl
}